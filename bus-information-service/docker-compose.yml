services:
  postgres:
    image: postgres
    container_name: ms_postgres
    environment:
      POSTGRES_DB: bus_management_db
      POSTGRES_USER: user_si
      POSTGRES_PASSWORD: 1999
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: